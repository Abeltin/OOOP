/*Абельтин С.А. 41ИС-21
3.1. Перегрузка имён функций: декорирование
Задача: Написать два метода с одним и тем же именем add, которые складывают либо два целых числа, либо два числа с плавающей точкой.*/

#include <iostream>

class Calculator {
public:
    int add(int a, int b) { return a + b; } // Первая версия функции
    double add(double a, double b) { return a + b; } // Вторая версия функции
};

int main() {
    Calculator calc;
    std::cout << "Сумма целых чисел: " << calc.add(2, 3) << std::endl; // Вызовет int add
    std::cout << "Сумма вещественных чисел: " << calc.add(2.5, 3.7) << std::endl; // Вызовет double add
    return 0;
}

/*Перегрузка функций позволяет использовать одно имя для нескольких функций, если у них разные параметры. Компилятор сам решает, какую функцию вызвать, исходя из переданных аргументов.
В C++ это реализуется с помощью декорирования имён: компилятор изменяет имена функций так, чтобы они включали типы параметров.*/
