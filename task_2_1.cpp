/*Абельтин С.А. 41ИС-21
2.1. Функции-члены (методы)
Задача: Написать класс Circle с методом getArea, который возвращает площадь круга.*/

#include <iostream>
class Circle {
private:
    double radius;
public:
    Circle(double r) : radius(r) {} // Конструктор инициализирует радиус
    double getArea() { return 3.14159 * radius * radius; } // Метод возвращает площадь
};

int main() {
    Circle circle(5.0); // Создание объекта круга
    std::cout << "Площадь: " << circle.getArea() << std::endl; // Вызов метода
    return 0;
}

/*Функции-члены (методы) — это функции, которые принадлежат объектам класса и могут работать с их данными.
В примере getArea является методом класса Circle, который рассчитывает площадь, используя радиус, хранящийся в объекте.*/
