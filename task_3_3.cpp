/*Абельтин С.А. 41ИС-21
3.3. Конструктор умолчания. Массивы объектов
Задача: Создать класс Box с конструктором умолчания и создать массив объектов этого класса.*/

#include <iostream>

class Box {
private:
    double width, height, depth;
public:
    Box() : width(1), height(1), depth(1) {} // Конструктор умолчания
    void print() {
        std::cout << "Box: " << width << " x " << height << " x " << depth << std::endl;
    }
};

int main() {
    Box boxes[3]; // Создание массива объектов, вызов конструктора умолчания
    for(int i = 0; i < 3; i++) {
        boxes[i].print();
    }
    return 0;
}

/*Конструктор умолчания — это конструктор, который можно вызвать без аргументов. Он полезен для создания массивов объектов.
Если конструктор умолчания не определен, компилятор не сможет создать массив объектов.*/
